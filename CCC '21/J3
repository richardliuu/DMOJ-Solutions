total = 0
direction = ""

while True:
    n = input()

    if n == "99999":
        break

    total = int(n[0]) + int(n[1])
        
    if total == 0:
        pass

    elif total % 2 == 0:
        direction = "right"
    else:
        direction = "left"
    
    print(f"{direction} " + n[2:])
